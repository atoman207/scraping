# Seller Scope を超えるための実装計画

前提となる調査結果: [SELLERSCOPE_ANALYSIS.md](./SELLERSCOPE_ANALYSIS.md)

---

## 実装済み（2026-09-03）

### 1. 原価・利益の計算エンジン ✅

**`lib/engine/cost.ts`** を新規作成。既存の `lib/db.ts` の式（元Python版と一字一句同じ、READMEの約束）は**触らずに残し**、上位互換の新エンジンとして独立させた。

Seller Scope の実データで**表示金額と完全一致**することを検証済み（`npm run test:cost` / 27件全通過）:

| 検証項目 | Seller Scope の表示 | 本実装 |
|---|---|---|
| 着地原価（デジタルスケール） | ¥244 | ¥244 ✅ |
| 1個利益 | +¥141 | +¥141 ✅ |
| 予想月販（回転0.1日） | 60個 | 60個 ✅ |
| 月利益 | +¥8,460 | +¥8,460 ✅ |
| 黒字ライン仕入値（売価950・送料160） | ¥695 | ¥695 ✅ |

**既存実装に足りていなかった6点を修正:**

| # | 不足していたもの | 影響 |
|---|---|---|
| 1 | 中国国内送料（工場→代行倉庫） | 原価が過小評価 |
| 2 | 関税（品目で0〜10%違う） | 衣類で原価が10%ズレる |
| 3 | 梱包費 | 利益が過大評価 |
| 4 | 回転日数の下限 | **回転0.1日で月販300個 → 月利益が5倍に膨らむ** |
| 5 | 予想月販の手入力 | 自動計算を上書きできない |
| 6 | 原価の直接入力モード | 国内せどりで使えない |

**Seller Scope より良くした4点:**

- **A. 輸入消費税を任意で計上**できる（`import_tax_pct`、既定0＝Seller Scope互換。彼らは未計上）
- **B. 月販数の根拠を返す**（`monthly_qty_source: 'manual' | 'rotation'`）。手入力か自動かを画面で区別
- **C. 原価の内訳を構造化して返す**（`CostBreakdown`）。画面でそのまま明細表示できる
- **D. 黒字ライン原価を「元」に逆算**（`breakEvenItemCny`）。**仕入交渉で直接使える**。Seller Scope は円でしか出さない
- E. 計算できない理由を日本語で返す（`reason: "送料入力待ち"` 等）。「取得できず」と「対象外」を区別する Seller Scope の設計を踏襲

### 2. DBスキーマの拡張 ✅

**`supabase/schema.sql`**（単一ファイル・何度実行しても安全）

- `settings`: `import_tax_pct` を追加
- `deepdive_items`: `cost_mode` `cost_direct_jpy` `china_domestic_cny` `tariff_cat` `box_count` `packaging_jpy` `monthly_qty` `sell_price_jpy` `shipping_jpy` を追加
  - **`box_count`（箱入数）と `order_qty`（発注数）を分離**。従来は `order_qty` 1本で兼用していたが別物
  - `china_domestic_cny`（中国国内送料/元）は既存の `domestic_shipping_jpy`（日本国内の発送料/円）とは別物なので新設
- `deepdive_view` を作り直し。**計算式はSQLに持たせず「素材を1行に揃える」だけ**にした（SQLとTSに式が二重にあると画面とCLIで数字がズレるため）

**適用方法**（`DATABASE_URL` 未設定のため手動）:
```bash
npm run db:push             # DATABASE_URL があれば自動適用
```
→ 出力されたSQLを **Supabase ダッシュボード > SQL Editor** に貼って Run。
（`.env.local` に `DATABASE_URL` を設定すれば次回から自動適用）

### 3. 画面の対応 ✅

`app/deepdive-list/page.tsx` を新エンジンに接続。追加した入力欄:
原価の出し方 / 商品単価(元) / 中国国内送料(元) / 関税区分 / 箱入数 / 原価を直接(円) /
売価 / 送料 / 販売手数料 / 梱包費 / 予想月販 / 発注数

表示: **着地原価・黒字ライン原価（＋単価◯元まで）・1個利益・月利益（＋月◯個・手入力表示）**、
原価の内訳を1行で明細表示、`鉄板 出品◯回` バッジ、入力待ちの理由表示。

**確認済み**: `npx tsc --noEmit` 通過 / `npx next build` 成功 / `npm run test:cost` 27件全通過。

---

## 未実装（優先度順）

### 優先度1: 非同期ジョブ基盤の永続化 🔴

**現状の問題**: `lib/jobs.ts` はジョブを**メモリ上のMapにしか持っていない**。
`// 状態はメモリ上にしか持たない(= サーバーを再起動すると履歴は消える)` とコメントにある通りで、
- サーバー再起動・デプロイで実行中ジョブが消える
- Vercel等のサーバーレスでは**そもそもインスタンスが分かれて動かない**
- 履歴画面が作れない

**やること**: `jobs` テーブルを作り、ワーカーを分離する。

```sql
CREATE TABLE jobs (
  id bigserial PRIMARY KEY,
  user_id bigint,
  seq integer,                  -- ユーザーごとの連番。画面の「#40」表示用
  type text,                    -- 'keyword' | 'deep' | 'sourcing'
  params jsonb,
  status text,                  -- 'queued' | 'running' | 'done' | 'error'
  progress jsonb,               -- { phase, i, n, label }
  result jsonb,
  error text,
  created_at timestamptz, started_at timestamptz, finished_at timestamptz
);
```
- 進捗の `phase` は Seller Scope に倣う: `crawl`(i/n) → `names` → `convert` / `deep`(i/n) → `ship`
- ポーリング間隔は **2,800ms**（Seller Scope の実測値。短すぎず遅すぎない）
- 順番待ちは**件数だけ表示し、前の人の待ち時間は約束しない**
- 「このページを閉じても処理は続きます」を明示

**さらに良くする点**: Seller Scope はポーリングのみ。**SSE（Server-Sent Events）にすれば進捗が即時に届く**。

### 優先度2: スクレイピングのスロットリング設計 🔴

Seller Scope の内部定数から実際の設計が読み取れた（`SELLERSCOPE_ANALYSIS.md` §9）:

```
ページ送り間隔     5秒
ワード間隔        10秒
セラー間クールダウン 10秒
1商品の実送料取得  8.5〜11秒
検索は7ページぶん
```
**実測: 1ワードあたり約30秒**（1ワード×1あるあるワード）。

`lib/scraper/browser.ts` にこの水準の待機が入っているか要確認。ブロック回避の実績値なので**そのまま採用してよい**。

**さらに良くする点**: Seller Scope は所要時間見積りを**上振れ側に固定係数**で出しているだけ。
実行実績を `jobs` テーブルに貯めて**実測ベースで見積もれば精度が上がる**。

### 優先度3: 予想月販の精度 🟡

Seller Scope は `30 ÷ 回転日数` のみ。回転日数は**そのセラーの実績**なので、
自分が出品したときに同じ回転で売れる保証がない。

**さらに良くする点**:
- 同一商品を扱う**セラー数（競合数）**で割り引く
- 実際の `sold_count`（観測できた販売数）を上限のサニティチェックに使う
- 「楽観・標準・悲観」の3本で出す

計算エンジン側は `monthly_qty` の手入力で上書きできるので、土台はすでにある。

### 優先度4: クレジット/クォータ管理 🟡

個人利用なら不要。**他人に配る場合のみ**必要。
Seller Scope の設計（実行前に「何回分消費・残り何回・実際に検索する組み合わせ一覧」を必ず提示）は、
クォータが無くても**実行前確認として有用**。組み合わせ爆発（10ワード×3パターン=30回）に気づける。

### 優先度5: ワードバンク 🟢

Seller Scope は **20ジャンル・1,568語**を内蔵し「サンプルから選ぶ」で入力補助。
静的JSONで足りるうえ、入力ハードルを大きく下げるので**費用対効果が高い**。
ジャンル一覧は `SELLERSCOPE_ANALYSIS.md` §4 に記載。

### 優先度6: 仕入先の画像検索連携 🟢

Seller Scope は Chrome拡張で AliExpress・1688 を**画像検索**して候補を取り込む（差別化の肝）。
現状 `lib/scraper/sourcing.ts` は**タイトル文字列での検索**。
画像検索の方が精度は高いが、拡張機能の開発・配布が必要で最も重い。**後回しでよい**。

---

## 設計として取り入れる価値があるもの

Seller Scope の実装から学べる、真似する価値のある判断:

1. **「取得できず」と「—（対象外）」を区別する** — システムの失敗と仕様上の対象外は別物。`ship_status` を `got/fixed/failed/na/skip` の5値で持つ設計は良い
2. **回転日数に下限0.5日** — 無いと月販数が発散する（実装済み）
3. **後追いアップグレード** — まず浅く広く（標準3商品）→ 気になるものだけ深く（+1で20商品）。無駄が出ない
4. **`/api/me` 一本で画面状態を返す** — user / settings / quota / billing / queue をまとめて返す
5. **未契約は 402 で返す** — フロントが申込導線に流せる
6. **更新履歴を「平易な説明＋折りたたみの技術的詳細」の二段構え**にする — 非エンジニア向けの良い書き方
7. **実行前に実際の検索語を全部列挙して確認を取る**
