# VPS運用ガイド（Supabase + VPS）

## 結論：Supabaseのままで問題ありません

**「ローカルDBに変えるべきか？」への回答：変えなくて大丈夫です。今のままを推奨します。**

理由を数字で示します。

| 判断材料 | 実測・実数 |
|---|---|
| **容量** | セラーリサーチ1回で約0.27MB。無料枠500MBなら**あと約1,870回ぶん**。月30回のペースで**5年以上**もちます（`npm run db:stats` で常に確認できます） |
| **API回数** | 無料プランでも**無制限**。ワーカーのポーリングは問題になりません |
| **転送量** | 月5GB。この規模の読み書きなら余裕があります |
| **自動停止** | 7日間アクセスが無いと停止しますが、**ワーカーが常駐していれば止まりません** |

**移行しない方がよい理由**：現在のコードは `supabase-js`（PostgREST経由）で書かれています。ローカルPostgreSQLに移すと**全てのクエリをSQLに書き直す**ことになり、動いているものを壊すリスクだけを負って、得られるものがほとんどありません。

**Supabaseを残す積極的な理由**：**VPSが壊れてもデータが残ります。** VPS内のDBだと、VPSの障害・再構築でデータごと消えます。リサーチの蓄積は資産なので、ここは分けておく価値があります。

### ただし、無料枠には自動バックアップがありません ⚠️

これが唯一の実害あるリスクです。**対策を実装しました**（下記の `npm run db:backup`）。毎日自動で取るよう登録してください。

### ローカルDBに切り替えるべきタイミング

以下に当てはまったら検討してください。それまでは不要です。

- `npm run db:stats` で **400MB を超えた**（＝残り2割）
- 月300回以上リサーチする運用になった（半年で上限）
- 保存を数秒でも速くしたい（VPS→Supabaseは1往復あたり数十ms）

その時は `pg` パッケージが既に入っているので、`lib/supabase.ts` を差し替える形で移行できます。**今やる必要はありません。**

---

## 今回VPS向けに直したこと

| 直した点 | 内容 |
|---|---|
| **セラー登録の往復が多すぎた** | 1人ずつ「検索→無ければ作成」で60人＝120往復していた。まとめて登録するようにして**2往復**に。VPS→Supabaseはネットワーク越しなので効きます |
| **ワーカーの無駄な問い合わせ** | 5秒固定＝1日17,000回。ジョブが無い間は最大1分まで間隔を広げるように（ジョブが来たら即座に5秒へ戻る） |
| **バックアップが無かった** | `npm run db:backup` を追加。毎日自動実行を登録できます |
| **`db:stats` の誤表示** | テーブルが存在しないのに「0行」と表示していた（無いのか空なのか区別できなかった）。「未作成」と出るよう修正 |

---

## セットアップ

### 1. DBのマイグレーション（まだ未実施です）

```bash
cd tenbai-next
npm run db:check     # 今どこまで当たっているか確認
```
現状は **002 / 003 / 004 がすべて未適用** です。

```bash
npm run db:migrate   # supabase/_migrations-merged.sql を書き出す
```
→ **Supabase ダッシュボード > SQL Editor** に中身を貼って **Run**

| ファイル | 内容 |
|---|---|
| `002_cost_model.sql` | 原価モデル（関税・中国国内送料・梱包費） |
| `003_listing_signals.sql` | 新品判定・更新日時（新品率と回転日数に必要） |
| `004_jobs.sql` | ジョブキュー（画面とワーカーの受け渡し） |

**確認**：
```bash
npm run db:check     # 「すべて適用済みです」と出れば完了
```

> `.env.local` に `DATABASE_URL`（Supabase > Project Settings > Database > Connection string の URI）を
> 設定すると、次回から `npm run db:migrate` が**自動で当たります**。手貼りは今回だけで済みます。

### 2. ブラウザ本体（初回のみ）

```bash
npx playwright install chromium
```

### 3. 起動

**画面**（別ウィンドウで）
```bash
npm run build
npm run start          # http://localhost:3000
```

**ワーカー**（別ウィンドウで）
```bash
npm run worker
```

ワーカーが動いていないと、画面からリサーチを実行しても順番待ちのまま進みません。

### 4. 自動起動の登録（VPS再起動後も動くように）

`ops/install-tasks.cmd` を**右クリック →「管理者として実行」**

| タスク名 | 内容 | タイミング |
|---|---|---|
| `TenbaiApp` | 画面 | ログオン時 |
| `TenbaiWorker` | ワーカー（落ちても自動で再起動） | ログオン時 |
| `TenbaiBackup` | DBバックアップ | 毎日 4:00 |

> 「ログオン時」なので、VPSにログオンした状態を保つ必要があります。
> 誰もログオンしていなくても動かしたい場合は、`/sc onstart` に変えて実行ユーザーとパスワードを指定してください（スクリプト内に例を書いてあります）。ワーカーは headless なので画面の無いセッションでも動きます。

---

## 日々の運用

```bash
npm run db:stats                 # 容量の減り具合。400MBを超えたら移行を検討
npm run db:backup                # 手動バックアップ
npm run db:backup -- --list      # 取得済み一覧
npm run db:backup -- --restore backups/2026-09-03_2033   # 復元
```

バックアップは `backups/日時/テーブル名.json` に出ます。**Supabaseの無料枠には自動バックアップが無いので、これが唯一の復旧手段です。** VPSごと壊れる事態に備えて、ときどき手元にもコピーしておくことをおすすめします。

### 動作確認

```bash
# ワーカーを起動しておいた状態で
curl -X POST http://localhost:3000/api/jobs \
  -H "Content-Type: application/json" \
  -d '{"kind":"search","keyword":"スマホ スタンド","pages":2,"sellers":10}'
# → {"jobId":1,"seq":1,"label":"リサーチ：スマホ スタンド（2ページ）"}

curl "http://localhost:3000/api/jobs?id=1"
```

DBを使わずスクレイパー単体で試すなら：
```bash
npm run scrape:search -- --keyword "スマホ スタンド" --pages 2 --dry
```

---

## 環境変数（`.env.local`）

| 変数名 | 用途 | 必須 |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | SupabaseのプロジェクトURL | ✅ |
| `SUPABASE_SERVICE_ROLE_KEY` | service_role キー（**公開厳禁**） | ✅ |
| `DATABASE_URL` | マイグレーションの自動適用に使用 | 推奨 |
| `BASIC_AUTH_USER` / `BASIC_AUTH_PASS` | 画面のBasic認証 | 外部公開するなら必須 |
| `SCRAPE_INTERVAL_MS` | ページ送りの間隔（既定5000）。短くするとブロックされやすい | – |
| `SCRAPE_COOLDOWN_MS` | ブロック後の待機（既定600000＝10分） | – |

> VPSの画面を外部に公開する場合は、**Basic認証を必ず設定**してください（未設定だと誰でも開けます）。
> 自分だけが使うなら、`npm run start` を localhost に限定し、Cursorのポート転送経由で見るのが安全です。

---

## GitHub

リポジトリ: `https://github.com/atoman207/scraping`（**現在パブリックです**）

コミットは済んでいますが、**push は認証が必要なため手動でお願いします**：
```bash
cd tenbai-next
git push origin main
```

> ⚠️ **公開設定の確認をおすすめします。** 現在は誰でもソースを閲覧できます。
> 商用ツールであれば Settings > General > Danger Zone から private への変更をご検討ください。
> なお `.env.local` は追跡対象外、`.env.example` の履歴にある値は実鍵と不一致（プレースホルダ）なので、**鍵の漏洩はありません**。

---

## 既知の制限（発注仕様書 5.）

| 項目 | 状況 |
|---|---|
| **売却日時** | メルカリが公開していないため取得不可。最終更新日時を `listings.updated_at` に保存し、回転日数の**推定**に使用。値下げ等でも更新される点に注意 |
| **セラー名** | 検索結果に含まれないため、集計後の上位N人だけプロフィールを開いて取得（既定60人）。それ以外はIDのまま |
| **実送料** | 3-2 の `getRealShippingCost` で取得。普通郵便・定形外・取引未完了は仕様上取得不可 |
| **メルカリShops** | 通常出品と名前空間が違うため `shops:` を付けて区別 |
| **サイト構造の変更** | 検索レスポンスの形が変わると0件になる。その場合ジョブはエラーで止まり「検索結果を取得できませんでした」と記録される（黙って壊れない設計） |
| **アクセス制限** | 403/429/503 を受けたら `BlockedError` で即停止し、ワーカーは10分待機 |
| **3-2 のワーカー対応** | 未実装。CLI（`npm run scrape:seller`）は動きます |
| **画面のジョブ連携** | 画面はまだ旧 `/api/scrape` を使用。新しい `/api/jobs` への接続が残っています |

## 規約についての技術者見解（発注仕様書 6.）

- 取得しているのは**ログイン不要で誰でも見られる公開ページの情報のみ**です。会員限定情報や個人情報（氏名・住所等）は取得していません。
- 認証やアクセス制限を迂回する処理は入れていません。403等を受けたら**停止します**。
- 取得方法は「検索ページを普通に開き、そのページが描画のために受け取ったデータを読む」もので、**追加のリクエストは発生しません**（アクセス回数＝ページを開く回数）。
- 一方、メルカリの利用規約には自動化されたアクセスを制限する条項が置かれるのが一般的です。**規約解釈と実施可否の最終判断は発注者様側でお願いします**（仕様書6.のとおり）。商用提供の際は事前のご確認を強くおすすめします。
