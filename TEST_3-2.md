# 3-2 セラー深掘り — 動作確認の手順と既知の制限

発注仕様書 3-2（`SellerDeepdiveAdapter`）が仕様どおり動くことを確認する手順です。
**上から順に実行すれば、DBを汚さずに確認 → 実データ投入 → 画面表示 まで進めます。**

対象は仕様書の2つのメソッドです。

| 仕様書 | 実装 |
|---|---|
| `get_seller_listings`（指定セラーの出品一覧を売却済み中心に新しい順で最大100件） | `MercariScraper.getSellerListings()` |
| `get_real_shipping_cost`（取引完了済みの商品ページから実送料） | `MercariScraper.getRealShippingCost()` |

---

## 事前準備（1回だけ）

```bash
cd tenbai-next

# 1. ブラウザ本体を入れる
npx playwright install chromium

# 2. DBスキーマを適用する（listings / sellers / product_groups に列を追加しています）
npm run db:push
#    DATABASE_URL 未設定なら supabase/schema.sql を Supabase の SQL Editor に貼って Run

# 3. 適用できたか確認（ここが OK にならないと保存で失敗します）
npm run db:check
```

期待する出力:

```
  OK  listings                   7列を確認
  OK  sellers                    5列を確認
  OK  product_groups             12列を確認
  ...
  OK 9件 / NG 0件
  すべて適用済みです。
```

> 適用前でも画面は開けるようにしてあります（足りない列は空欄になり、
> 「DBの更新がまだ適用されていません」という案内が出ます）。ただし
> **取得結果の保存は失敗する**ので、実行する前に適用してください。

---

## テスト1: ネットにつながずロジックだけ確認（数秒）

実際にメルカリから受け取ったレスポンスの断片を入力にして、解析ロジックだけを検証します。
**サイトの状態に左右されないので、「サイトが変わったのか、コードが壊れたのか」の切り分けに使えます。**

```bash
npm run test:3-2
```

期待する出力:

```
===== 結果: 42件OK / 0件NG =====
```

確認している内容:

- 出品一覧 … 売却判定（`sold_out`/`trading` を売却に数える）・出品日時/最終更新日時のISO変換・
  配送方法IDから名前を引く・非公開商品の除外・壊れた行の除外・続きを読むカーソル
- 並び順 … 出品日時の降順（＝新しい順）になること
- メルカリShops … 商品IDの取り出し・在庫なしを売り切れ扱いにすること
- プロフィール … 名前・★評価・評価件数・総出品数・**アバター（既定画像はNULL扱い）**
- 実送料 … 後述の5状態すべて

---

## テスト2: 実際のメルカリで精度を確認（1〜2分）

セラーページから取ったデータ（A）を、**商品ページの実物（B）** と1件ずつ突き合わせます。
AとBは取得経路も解析コードも別なので、一致すれば「たまたま動いている」のではないと言えます。

```bash
npm run verify:3-2 -- --seller 223868190 --sample 4 --shipping 3
```

オプション:

| | |
|---|---|
| `--seller` | セラーID。メルカリShopsは `shops:<店舗ID>`。プロフィールURLを貼っても動きます |
| `--max` | 取得する出品数の上限（既定100 = 仕様書の上限） |
| `--sample` | 商品ページと突き合わせる件数（既定5） |
| `--shipping` | 実送料を取りに行くSOLD上位件数（既定3） |
| `--interval` | アクセス間隔ミリ秒（既定5000） |

実際の出力（2026-09-04 実行）:

```
=== 3-2 の精度検証 ===
  セラー: 223868190 / 最大100件 / 標本4件

[1] セラーページから出品一覧を取得します…
    … 30件
    … 58件
  セラー名: しずく
  ★評価: 4.5 / 評価件数: 106件
  取得: 58件 (SOLD 53件 / 販売中 5件)
  メルカリ側の総出品数(販売中): 60件
  並び順: 出品日時が取れた 58件中、逆転 0件 → 新しい順
  発送済み: 52件以上 (一覧に出た分だけ) / 購入済みで未発送: 1件

[2] 4件の商品ページを開いて突き合わせます…

  [1/4] OK   m46719044146  僕のヒーローアカデミア THE EVIL VILLA
  [2/4] OK   m18559579051  COACH シグネチャー アップル柄 ポーチ
  [3/4] OK   m41016211257  be Answer スカルプケアエッセンス 70mL
  [4/4] OK   m86958292340  キルティング ショルダーバッグ ミニポーチ付き

=== 突き合わせ結果 (4件を照合) ===

  項目        一致  相違  判定不能
  タイトル　　　　　　    4    0     0
  価格　　　　　　　　    4    0     0
  売却　　　　　　　　    4    0     0
  出品者　　　　　　　    4    0     0
  出品日時　　　　　　    4    0     0
  発送方法　　　　　　    4    0     0

[3] 実送料をSOLD上位3件について取得します…

  [1/3] 実送料を取得 ¥210  発送時に確定した実送料(ネコポス ¥210)
  [2/3] 実送料を取得 ¥210  発送時に確定した実送料(ネコポス ¥210)
  [3/3] 実送料を取得 ¥210  発送時に確定した実送料(ネコポス ¥210)

  内訳: 取得3件 / 一律0件 / 取れず0件 / 対象外0件

=== 判定 ===
  合格。セラーページから取れたデータは商品ページの実物と一致しています。

  所要 63秒
```

他に確認したパターン（いずれも合格）:

| セラー | 確認できたこと |
|---|---|
| `223868190`（58出品） | 全件取得・実送料3/3件取得 |
| `350845924`（3,297出品） | **上限100件で正しく打ち切る**・追加読み込みのページ送り・発送前の出品は「取得不可」 |
| `597663090` | ゆうゆうメルカリ便で実送料6/6件取得 |
| `shops:waKfjvmcR3eg7r4eL3xS8b` | メルカリShopsのセラーも100件取得・店名/★評価/レビュー件数 |

---

## テスト3: DBに書かずに取得結果だけ見る（1分）

```bash
npm run scrape:seller -- --seller 223868190 --max 40 --shipping 2 --dry-run
```

同じセラーの同じ商品が繰り返し出品されていれば、**鉄板商品**の候補です。

```
  SOLD ¥1,100 2026-04-17 《新品》プルームテックプラスwith　純正カートリッジ… 送料¥210(ネコポス)
  SOLD ¥1,350 2026-04-13 《新品》プルームテックプラスwith　純正カートリッジ…
  SOLD ¥1,350 2026-03-16 《新品》プルームテックプラスwith　純正カートリッジ…
  …
```

---

## テスト4: DBに投入して画面で見る（1〜2分）

```bash
npm run scrape:seller -- --seller 223868190
```

`--max`（既定100）件を取得 → 実送料を上位`--shipping`（既定3）件だけ取得 → DBへ投入 →
鉄板商品の抽出まで一気に流れ、最後に画面のURLが出ます。

```
完了。 http://localhost:3000/seller-deepdive?seller_id=12 を開いてください。
```

Web画面から実行する場合は、セラーリサーチ結果の「深掘りへ」から実行できます
（`app/api/scrape/route.ts` の `runSeller` が同じ処理を呼びます）。

---

## どこから何を取っているか

3-1 と同じ方針で、**ページを普通に開いた結果としてブラウザが受け取ったレスポンス**を読みます。
こちらから内部APIを叩くのではないので、アクセス回数は「ページを開く回数」＋
「利用者がスクロールしたときの追加読み込み」だけです。

| 開くページ | 読むもの | 取れる項目 |
|---|---|---|
| `/user/profile/<セラーID>` | `users/get_profile` | 名前・**プロフィール画像**・★評価・評価件数(良/悪)・総出品数・登録日 |
| 同上（スクロールで続きを読む） | `items/get_items` | 商品ID・タイトル・価格・売却状態・**出品日時**・**最終更新日時**・**配送方法**・**取引状態** |
| `/shops/profile/<店舗ID>` | `bff/shops/v1/contents/shops/<id>` | 店名・**店舗ロゴ**・★評価・レビュー件数・開設日 |
| 同上 | `bff/shops/v1/shops/<id>/products` | 商品ID・タイトル・価格・在庫有無・登録日時 |
| `/item/<商品ID>` | `items/get` | **実送料（`shipping_class`）**・配送方法・商品の状態・取引状態 |

### プロフィール画像（アバター）

メルカリは画像を設定していないセラーに、**全員同じ既定画像**（`member_photo_noimage.png`）を返します。
そのまま並べると一覧で見分けがつかないので、既定画像は `NULL` として保存し、
画面では**名前の頭文字を色付きの丸**で描いています（色は名前から決まるので、同じセラーは常に同じ色）。

実データでの確認（2026-09-04）:

```
[223868190]   しずく                          avatar=(既定画像 → 頭文字表示)
[350845924]   2点1000円♡フォロワー様特典あり     avatar=https://static.mercdn.net/thumb/members/webp/350845924.jpg
[597663090]   MASA                            avatar=https://static.mercdn.net/thumb/members/webp/597663090.jpg
[shops:waKf…] Sundear　メルカリ店              avatar=https://assets.mercari-shops-static.com/-/small/plain/….jpg
```

#### プロフィールを取りに行っていないセラーでも画像を出す

3-1のセラーリサーチは「集計して上位N人だけ名前を引く」設計なので、
**画像URLを保存できるのは一部のセラーだけ**です。DBだけを見ていると、ほとんどのセラーが
頭文字表示になってしまいます。

通常セラーの画像URLは、セラーIDから一意に決まります(2026-09-04 に確認)。

```
https://static.mercdn.net/thumb/members/webp/<セラーID>.jpg
  画像を設定しているセラー → 200 / image/webp
  設定していないセラー     → 403
  参照元(Referer)の制限は無し。キャッシュ用のクエリ文字列も不要
```

そのため画面では、**保存済みの値が無ければIDから組み立てて**表示します(`lib/avatar.ts`)。
実際のDBで確認したところ、保存済み0件の状態でも **43人中42人**に画像を出せました
(残り1人はメルカリShopsで、店舗ロゴはIDから決まらないため保存値が要ります)。

画像URLは後から失効することがあるので、読み込みに失敗したら頭文字に切り替わります。

### 参考にした既存サービスより多く取っているもの

深掘りの結果（`product_groups`）には、参考サービスの行スキーマに無い項目を足しています。

| 項目 | 何が分かるか |
|---|---|
| `stock_count` | 今も販売中の在庫があるか（0なら扱いをやめた可能性） |
| `min_price` / `max_price` | 価格レンジ。値下げ幅が大きい商品は利益がぶれる |
| `first_listed_at` | いつから回している商品か。長いほど鉄板度が高い |
| `latest_sold_at` | 直近で売れた日。古ければ今は売れていない |
| `sold_per_month` | **実測の月販数**。観測期間と実際の売却数から出すので、`30 ÷ 回転日数` の推定より確か |
| `distinct_title_count` | タイトルを何種類に変えて出し直しているか |
| `merged_titles` | 束ねた実際のタイトル一覧。グルーピングが妥当か画面で確認できる |
| `ship_status` / `ship_class` | 実送料が「取れなかった」のか「対象外」なのか、どのサイズ区分だったか |
| セラーの `good_ratings` / `bad_ratings` / `registered_at` | 評価の内訳と出品歴の長さ |

レスポンスが読めなかった場合は、**画面（DOM）から読む従来の方法に自動で切り替わります**。
その場合はタイトル・価格・売り切れだけが取れ、出品日時や実送料は取れません。

### なぜDOMではなくレスポンスを読むのか

セラーページの一覧DOMには、タイトル・価格・売り切れバッジしか出ません。
出品日時・配送方法・取引状態がDOMに無いため、**DOMだけでは回転日数も実送料の取得可否も判定できません**。

---

## 実送料の5状態

「取得できなかった」と「そもそも対象外」は別物なので、`listings.ship_status` で区別して保存します。

| 状態 | 意味 | 画面での出し方 |
|---|---|---|
| `got` | 発送時に確定した実送料を取得できた | 金額を出す |
| `fixed` | 全国一律なので金額が確定している（クリックポスト185円・着払い0円） | 金額を出す |
| `failed` | メルカリ便で発送済みなのに金額が公開されていなかった | 「（送料待ち）」 |
| `na` | 仕様上そもそも取れない（下表） | 「—」 |
| `skip` | そのモードでは取得対象にしていない（上位N件から外れた） | 「—」 |

### 実送料が確定するのは「発送された時点」

取引完了（購入者の評価まで済む）を待ちません。
発送済みで評価待ち（`wait_review`）の段階でも金額は確定しています。
逆に、支払待ち・発送待ちの段階では確定していません。

コード上も、取引状態の文字列ではなく**金額が入っているかどうか**で判定しています
（メルカリ側の状態の語彙が増えても壊れないようにするため）。

---

## 既知の制限事項

### 実送料が取れないケース（仕様書の「取得不可でよい」に該当）

| ケース | 理由 |
|---|---|
| 普通郵便・定形外・レターパック・ゆうメールなど | メルカリが配送を担当しないため、金額が公開されない |
| 発送方法「未定」 | まだ決まっていない |
| 購入されたがまだ発送されていない | 発送時に確定するため |
| 販売中（まだ売れていない） | 取引が始まっていない |
| メルカリShopsの商品 | 店舗が独自に配送を手配するため、公開されない |

### 取得のしかたに由来する制限

- **売却日時（`sold_at`）は取得できません。** メルカリが公開していないためです。
  代わりに最終更新日時（`updated_at`）を売却時刻の目安として保存し、
  回転日数は「出品日時 → 最終更新日時」で推定しています（3-1 の集計と同じ扱い）。
- **商品の状態（新品/中古）は出品一覧に含まれません。** 一覧から取った出品の `is_new` は `NULL` です。
  商品ページを開いた分（＝実送料を取りに行った上位N件）だけは取得できます。
- **メルカリShopsは取れる項目が少なくなります。**
  売却の判定が「在庫あり/なし」しかないため、**売り切れと在庫切れを区別できません**。
  配送方法・実送料・取引状態も公開されていません。
- **出品者が非公開にした商品は除外しています**（セラーページの表示に合わせるため）。
- 出品数が100件を超えるセラーでも、仕様どおり**新しい順に100件で打ち切ります**。
  それより古い出品は取得しません。

### 壊れたときの見分け方

| 症状 | 考えられる原因 | 確認 |
|---|---|---|
| `npm run test:3-2` がNG | こちらのコードの不具合 | 変更箇所を戻す |
| `test:3-2` はOKだが `verify:3-2` がNG | メルカリ側の仕様変更 | ログの「レスポンスから出品を読み取れませんでした」を確認 |
| 「画面の一覧から取得します」が出る | レスポンスの形式かURLが変わった | `lib/scraper/mercari-seller.ts` の型と `mercari.ts` の捕捉パターンを見直す |
| 実送料が全件 `failed` | `shipping_class` の場所が変わった | `lib/scraper/mercari-seller.ts` の `realShippingOf()` |
| `[中断] サイト側にアクセスを拒否されました` | アクセスが多すぎる | `--interval 8000` などにして時間をおく |

---

## アクセス頻度について

- ページ遷移は既定 **2.5秒 + ゆらぎ最大1.2秒** の間隔を必ず空けます（`--interval` で変更可）。
- セラーページの追加読み込みは、**1回あたり1.2秒以上の間隔**を空けてスクロールします。
  1回で30件（Shopsは100件）読み込むので、100件なら追加読み込みは3回程度です。
- 実送料は商品ページを1件ずつ開くため、**上位N件だけ**に絞る設計です（既定3件、詳細モード20件）。
- 1セラーあたりの目安: ページを開くのは **1 + N回**（Nは実送料を取る件数）。
  既定（N=3）なら4ページ、詳細（N=20）なら21ページです。

---

## ブロックされないようにするために、やっていること／やっていないこと

「弾かれずに安定して取れる」ことと「ブロックを回避して押し通す」ことは別物なので、線を引いています。

**やっていること（弾かれる原因を取り除く）**

| 対策 | 理由 |
|---|---|
| 通常のChromeとしてUser-Agentを名乗る | Playwright既定は `HeadlessChrome/…` と自己申告し、**通常のページが配信されない**ことがある。バージョン番号は実行中のChromiumの実値をそのまま使う |
| `navigator.webdriver` を消す | 同上。立っていると簡易版のページが返り、「取得できたのに中身が空」という切り分けづらい失敗になる |
| `--disable-blink-features=AutomationControlled` | 同上 |
| `Accept-Language: ja-JP` / ロケール ja-JP / タイムゾーン Asia/Tokyo | 日本向けの検索結果と価格表記を得るために必要 |
| アクセス間隔 2.5秒＋ゆらぎ、並列アクセスなし | 相手に負荷をかけないため |
| 画像・フォント・広告/計測タグを読み込まない | 転送量と相手の負荷を下げるため |
| 通信の瞬断・タイムアウトを最大2回、待ち時間を倍にしながらやり直す | 一時的な失敗で処理全体が落ちないようにするため |

**やっていないこと（意図的に実装していない）**

- CAPTCHAの自動突破
- プロキシ／IPの切り替え
- ブラウザ指紋（Canvas・WebGL・フォント等）の偽装
- **ブロックされた後の再挑戦**

HTTP 403 / 429 / 503、またはブロックページを受け取った時点で `BlockedError` を投げて**その場で止めます**。
断られているのに叩き続けるのは相手に迷惑で、こちらの状況も悪化させるだけだからです。
止まったら `--interval` を広げて時間をおいて実行し直してください。

> 発注仕様書の非機能要件にあるとおり、規約解釈と実施可否の最終判断は発注者側でお願いします。

---

## 画面について

### セラーリサーチ（①）

- 各セラーに**アバター**を表示。画像が無いセラーは名前の頭文字を色付きの丸で出します
- **分類**（専門特化(穴場候補) / 中堅特化 / 複数展開 / 小規模・単発）を色分けしたピルで表示。
  「穴場候補」だけ橙で強調しています
- **回転日数・新品率**は、設定のNG基準を満たすかで緑／橙に色分け。
  基準を外れた行は**薄く表示**（消さずに残す）
- 上部に指標タイル（セラー数 / 総SOLD / 穴場候補 / NG基準）

### セラー深掘り（②）

- セラーのアバター・★評価・良い評価/悪い評価・総出品数・登録元へのリンク
- 商品グループごとに、**出品数・SOLD・平均価格・回転日数・実測月販・実送料・黒字ライン・最新SOLD・初回出品**
- 実送料は5状態を色で区別（取得済み=緑 / 送料待ち=橙 / 対象外=灰）。マウスを乗せると理由が出ます
- 「まとめた◯件のタイトルを見る」で、グルーピングの中身を確認できます
- 実行前に**標準（上位3件）／詳細（上位20件）**を選べます

### 待ち時間の見せ方

検索は数十秒〜数分かかります。止まって見えないよう、以下を出しています。

- **段階表示**: 検索結果を読む → セラー名を調べる → 集計する → 保存する（深掘りは 出品を読む → 実送料を調べる → 保存する → 鉄板商品を抽出）
- **進捗バー**: 何件中何件かが分かるときは実際の割合、分からないときは往復するバー
- **経過時間**と、いま処理している対象の名前
- 実行中のボタンに光が流れる／ログが自動で追従

いずれも **サーバーが返す実際の進捗**（`lib/jobs.ts` の `JobProgress`）に基づいています。
見た目だけ進んでいるように見せてはいません。
`prefers-reduced-motion`（OSで「視差効果を減らす」）を有効にしている場合、アニメーションは止まります。
